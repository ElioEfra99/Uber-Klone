type User @model {
  id: ID!
  accountType: Int!
  email: AWSEmail!
  fullName: String!
  location: Location @connection
}

type DriverLocation @model {
  id: ID!
  latitude: Float!
  longitude: Float!
}

type Location @model {
  latitude: Float!
  longitude: Float!
}
